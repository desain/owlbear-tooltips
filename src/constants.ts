const PLUGIN_ID = "com.desain.tooltips";
export const EXTENSION_NAME = "Tooltips";

// Broadcasts
export const CHANNEL_MESSAGE = `${PLUGIN_ID}/message`;

// Storage
export const LOCAL_STORAGE_STORE_NAME = `${PLUGIN_ID}/localStorage`;
export const METADATA_KEY_ROOM = `${PLUGIN_ID}/roomMetadata`;
export const METADATA_KEY_TOOLTIPS = `${PLUGIN_ID}/tips`;

// Tool
export const ID_MODE_TOOLTIP = `${PLUGIN_ID}/manageMode`;
export const ID_ACTION_SETTINGS = `${PLUGIN_ID}/openSettings`;

// Popover
export const ID_POPOVER_SETTINGS = `${PLUGIN_ID}/popoverSettings`;
export const ID_POPOVER_EDIT = `${PLUGIN_ID}/popoverEdit`;
export const URL_PARAM_ID = "id";
